---
# ServiceAccount with IRSA annotation so the pod can call cloudwatch:PutMetricData
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dora-exporter-sa
  namespace: monitoring
  annotations:
    # Overwritten by bootstrap.sh from the dora_exporter_role_arn Terraform output
    # immediately after this manifest is applied, so the literal value here only
    # matters for the brief window in between (and for anyone applying by hand).
    # Seed value only — bootstrap.sh re-annotates this ServiceAccount with the real
    # ARN from the Terraform output immediately after applying this file, and
    # setup.sh substitutes YOUR_AWS_ACCOUNT_ID during personalisation.
    eks.amazonaws.com/role-arn: "arn:aws:iam::YOUR_AWS_ACCOUNT_ID:role/idp-mvp-dora-exporter"

---
# ExternalSecret syncs GITHUB_TOKEN from Secrets Manager key: idp-mvp/dora-exporter
# Pre-requisite: create the secret manually first:
#   aws secretsmanager create-secret --name idp-mvp/dora-exporter \
#     --secret-string '{"GITHUB_TOKEN":"<your-github-pat>"}'
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: dora-exporter-secret
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: dora-exporter-secret
    creationPolicy: Owner
  data:
    - secretKey: GITHUB_TOKEN
      remoteRef:
        key: idp-mvp/dora-exporter
        property: GITHUB_TOKEN

---
# The Python script is stored in a ConfigMap so updates don't require an image rebuild.
# To update the script: edit observability/dora/dora-exporter.py, then:
#   kubectl create configmap dora-exporter-script \
#     --from-file=dora-exporter.py=observability/dora/dora-exporter.py \
#     -n monitoring --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: ConfigMap
metadata:
  name: dora-exporter-script
  namespace: monitoring
binaryData: {}  # populated by the kubectl create configmap command above
# data.dora-exporter.py: <script contents — do not edit here, use the command above>

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: dora-exporter
  namespace: monitoring
  labels:
    app.kubernetes.io/name: dora-exporter
    app.kubernetes.io/component: observability
spec:
  schedule: "*/15 * * * *"   # every 15 minutes
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      activeDeadlineSeconds: 600   # 10 min hard limit per run
      template:
        metadata:
          labels:
            app.kubernetes.io/name: dora-exporter
        spec:
          serviceAccountName: dora-exporter-sa
          restartPolicy: OnFailure
          initContainers:
            - name: install-deps
              image: python:3.13-slim
              command:
                - pip
                - install
                - --quiet
                - --target=/deps
                - boto3
                - requests
              resources:
                requests:
                  cpu: 200m
                  memory: 256Mi
                limits:
                  cpu: 500m
                  memory: 512Mi
              volumeMounts:
                - name: deps
                  mountPath: /deps
          containers:
            - name: dora-exporter
              image: python:3.13-slim
              command:
                - python
                - /scripts/dora-exporter.py
              env:
                - name: PYTHONPATH
                  value: "/deps"
                - name: GITHUB_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: dora-exporter-secret
                      key: GITHUB_TOKEN
                - name: GITHUB_ORG
                  value: "moatazeldebsy"
                - name: AWS_REGION
                  value: "us-east-1"
                # 7 days, the conventional DORA reporting window. 24h was too
                # narrow to be meaningful here: a service that shipped four
                # times on Tuesday and nothing on Wednesday reported 0.00
                # deploys/day, which reads as "broken pipeline" rather than
                # "no changes today", and left most services with no series at
                # all for their Backstage DORA tab to query.
                - name: LOOKBACK_HOURS
                  value: "168"
                - name: CLOUDWATCH_NS
                  value: "IDP/DORA"
                # Optional: map repo names to team names for per-team DORA metrics.
                # Falls back to repos tagged with GitHub topic "team:<name>" if unset.
                # Set TEAM_MAP in Secrets Manager under idp-mvp/backstage, then
                # add it to the ExternalSecret in aws/external-secrets/backstage.yaml.
                - name: TEAM_MAP
                  valueFrom:
                    secretKeyRef:
                      name: dora-exporter-secret
                      key: TEAM_MAP
                      optional: true
              resources:
                requests:
                  cpu: 100m
                  memory: 128Mi
                limits:
                  cpu: 500m
                  memory: 256Mi
              volumeMounts:
                - name: script
                  mountPath: /scripts
                - name: deps
                  mountPath: /deps
          volumes:
            - name: script
              configMap:
                name: dora-exporter-script
            - name: deps
              emptyDir: {}
