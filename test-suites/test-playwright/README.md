# test-playwright

Playwright E2E suite for `hello-service`.

```bash
npm install && npx playwright install --with-deps
npm test
npx playwright show-report
BASE_URL=http://staging.example.com npm test   # override target
```
