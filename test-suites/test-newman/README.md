# test-newman

Newman API test suite for `hello-service` at `http://localhost:3000`.

```bash
npm install && npm test
```

Import `collections/test-newman.postman_collection.json` into Postman to edit visually.
