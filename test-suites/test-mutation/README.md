# test-mutation

Stryker mutation testing for `hello-service`. Min score: **70%**.

```bash
npm install && npm test
# HTML report: reports/mutation/index.html
```

Point `stryker.config.js` at the target service source and test files before running.
