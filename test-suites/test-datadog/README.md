# test-datadog

Datadog synthetic tests for `hello-service` on `datadoghq.eu`.

```bash
npm install
DD_API_KEY=<key> DD_APP_KEY=<app_key> npm test
```

Set `DD_API_KEY` and `DD_APP_KEY` as GitHub repository secrets.
